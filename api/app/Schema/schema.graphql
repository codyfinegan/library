schema {
    query: Query
    mutation: Mutation
}

type Query {
    greetings(input: HelloInput!): String!
}

type Mutation {
    login(input: HelloInput!): String!
}

input HelloInput {
    firstName: String!
    lastName: String
}
